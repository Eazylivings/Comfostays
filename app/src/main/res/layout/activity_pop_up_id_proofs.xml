<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activities.owner_activities.tenant_activities.PopUpIdProofs"
    android:background="#ffffff">

    <ImageView
        android:layout_width="400dp"
        android:layout_height="400dp"
        android:id="@+id/popUpIdProofs_imageView"
        android:contentDescription="@string/description"
        android:layout_alignParentTop="true"
        android:scaleType="fitStart"
        android:adjustViewBounds="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="42dp"
        android:visibility="invisible" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/close"
        android:id="@+id/popUpIdProofs_button"
        android:layout_below="@+id/popUpIdProofs_imageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="31dp"
        android:onClick="onClickClose" />

    <ProgressBar
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar_imageLoadingProgress"
        android:layout_alignTop="@+id/popUpIdProofs_imageView"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
